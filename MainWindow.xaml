<Window x:Class="LCPSAutomate.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="250" Width="600">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,8">
            <Button x:Name="StartButton" Width="90" Margin="0,0,8,0" Click="StartButton_Click">开始监测</Button>
            <Button x:Name="StopButton" Width="90" Click="StopButton_Click">停止监测</Button>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Center" Margin="0,0,0,8">
            <TextBox x:Name="FolderTextBox" Width="420" Height="28" VerticalAlignment="Center" VerticalContentAlignment="Center" TextWrapping="NoWrap" IsEnabled="False"/>
            <Button x:Name="BrowseButton" Width="90" Margin="8,0,0,0" Click="BrowseButton_Click">选择文件夹</Button>
        </StackPanel>

        <TextBlock x:Name="StatusTextBlock" 
                   Grid.Row="3" 
                   Margin="0,12,0,0"
                   TextWrapping="Wrap" Foreground="LightGreen" />
    </Grid>
</Window>
